import 'package:flutter/material.dart';

class StudentBoardScreen extends StatelessWidget {
  const StudentBoardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('掲示板'));
  }
}
