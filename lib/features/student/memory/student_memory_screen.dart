import 'package:flutter/material.dart';

class StudentMemoryScreen extends StatelessWidget {
  const StudentMemoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('記憶'));
  }
}
